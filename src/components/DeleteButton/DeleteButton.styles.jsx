import { styled } from "styled-components";

export const DeleteButtonContainer = styled.div`
    cursor: pointer;
    & svg {
    width: 30px;
    height: auto;
    color: #3c3c3c;
    opacity: 0.9;
}
`
